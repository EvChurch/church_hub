query eventQuery($ids: [ID!]) {
  events(ids: $ids) {
    nodes {
      id
      name
      bannerUrl
      startAt
      endAt
      address
      content
      location {
        id
        name
      }
    }
	}
}
